---
description: 
globs: 
alwaysApply: true
---
OctoPrompt Python Code Rules
- use unix timestamp in milliseconds for all ids.
- created and updated are also in unix timestamp milliseconds
- instead of using created_at or createdAt timestamps are always saved as created and updated 
- uv is used with .venv
- uv pytest for testing
- when running python files in terminal use uv
- run test with `uv run pytest`
- activate env with `source .venv/bin/activate