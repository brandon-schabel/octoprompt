{
  "name": "@octoprompt/api-client",
  "version": "0.5.3",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "test": "bun test src/",
    "test:watch": "bun test --watch src/",
    "build": "tsc",
    "prepublishOnly": "bun run build && bun run test",
    "publish:dry": "npm publish --dry-run"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}