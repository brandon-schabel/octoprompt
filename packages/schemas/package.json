{
  "name": "@octoprompt/schemas",
  "version": "0.7.1",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "test": "bun test src/",
    "test:watch": "bun test --watch src/",
    "build": "tsc",
    "prepublishOnly": "bun run build && bun run test",
    "publish:dry": "npm publish --dry-run"
  },
  "files": [
    "dist",
    "src",
    "index.ts",
    "README.md"
  ],
  "dependencies": {
    "@octoprompt/config": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}